---
slug: Teste
title: Meu Primeiro Post
date: 2020-10-21
author: wutali
tags:
  - document
---
# Title

Esse **post** é apenas um teste.

> Aprendi na verdade aqui... [veja o site](https://woliveiras.com.br/posts/desenvolvendo-um-blog-com-interface-administrativa-com-hugo-e-netlify/)

`git add --all`

`git commit -m "teste"`

- - -

* Ponto importante

  * isso...

1. Aprecie
2. use com moderação

- - -

```
export const getStaticPaths: GetStaticPaths = async () => {
  const pages = Math.ceil(countPosts() / config.posts_per_page);
  const paths = Array.from(Array(pages - 1).keys()).map((it) => ({
    params: { page: (it + 2).toString() },
  }));
  return {
    paths: paths,
    fallback: false,
  };
};
```





![](/images/velter.jpg)